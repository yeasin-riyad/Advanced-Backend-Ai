events{

}

http{

    #   upstream backend {
    #     server server1:7000;
    #     server server2:7000;
    #     server server3:7000;
    #   }    

      server{
        listen 80;
        location /{
            # proxy_pass http://backend;
            return 200 "hello from nginx";
        }
      } 
}